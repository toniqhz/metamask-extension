import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { SelectWrapper } from './select-wrapper';

# SelectWrapper

SelectWrapper is a graphical element that allows users to select one or more options from a set of choices.

<Canvas>
  <Story id="components-componentlibrary-select-wrapper--default-story" />
</Canvas>

## Props

<ArgsTable of={SelectWrapper} />

### Demo

Use the `SelectWrapper` when starting a new DS component from scratch

<Canvas>
  <Story id="components-componentlibrary-select-wrapper--demo" />
</Canvas>

```jsx
import { SelectWrapper } from '../../component-library';

<SelectWrapper />;
```
